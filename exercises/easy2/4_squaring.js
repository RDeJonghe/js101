/* Using the multiply() function from the "Multiplying Two Numbers" problem, write a function that computes the square of its argument (the square is the result of multiplying a number by itself). */


// THIS NEEDS WORK, TRYING TO GO GET OTHER FILE.

function multiply(num1, num2) {
  let total = 0;
  total = num1 * num2;
  return total;
}

function square(number) {
  return multiply(number, number);
}

console.log(square(7));
/*
function powerTo(number, exponent) {
  return Math.pow(number, exponent);
}

console.log(powerTo(5, 2));
console.log(powerTo(3, 5));
console.log(powerTo(7, 3));
console.log(powerTo(2, 6));
console.log(powerTo(4, 3));
*/