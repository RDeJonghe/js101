console.log([1, 2, 3] + [4, 5]);

// I think this will concatenate the two arrays in to 1 array. Similar to using .concat
// never that simple.
// + converts them to strings and then concats the strings

console.log([1, 2, 3].concat([4, 5]));

// use .concat for that behavior of joining them

